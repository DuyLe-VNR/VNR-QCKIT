Tạo các file data catalog cho danh mục hệ thống dựa vào FlowDI để lấy ra các danh mục trong hệ thống (nếu có thì tạo đè lại). lưu tại
\.specify\tests\data-catalog\categories\ cấu trúc tương tự trong plugin: \.specify\templates\banks.json
Cách thức sử dụng mcp connect tới database để lấy dữ liệu, mỗi danh mục 20 dòng. Tạo đầy đủ toàn bộ các danh mục, danh mục nào không lấy được thì confirm lại.
